#include <iostream>
#include <rg_Circle2D.h>
#include <VoronoiDiagramCIC.h>
#include "heuristic_algorithm.h"

#include <list>

using namespace V::GeometryTier;

int main(int, char**) {
    HeuristicAlgorithm heuristic;


    // vector<int> a = {1,2,3};
    // vector<int> b = {4,5};
    // vector<int> c = {6,7,8};
    // vector<int> d = {9,10};

    // vector<vector<int>> zip = {a,b,c,d};

    // int start = 0;
    // int end = 3;

    // zip[start].insert(zip[start].end(), zip[end].begin(), zip[end].end());
    // zip.erase(zip.begin() + end);

    // for (auto it : zip[start]) {
    //     std::cout << it << std::endl;
    // }
    // std::cout << std::endl;
    // std::cout << zip.size() << std::endl;


    
    std::cout << "Hello, world!\n";
}
